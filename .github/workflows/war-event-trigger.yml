name: War Event Trigger (Force Wake)

on:
  schedule:
    - cron: "* * * * *"   # ทุก 1 นาที (UTC)
  workflow_dispatch:

jobs:
  wakeup-test:
    runs-on: ubuntu-latest

    steps:
      - name: Force schedule test
        run: |
          NOW=$(date -u +"%H:%M:%S")
          echo "UTC now: $NOW"

          curl -H "Content-Type: application/json" \
            -X POST \
            -d "{\"content\":\"🟢 Schedule ทำงานแล้ว เวลา UTC $NOW\"}" \
            ${{ secrets.DISCORD_WEBHOOK }}
