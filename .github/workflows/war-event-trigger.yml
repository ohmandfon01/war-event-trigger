name: War Event Trigger Test

on:
  schedule:
    - cron: "* * * * *"   # ทุก 1 นาที
  workflow_dispatch:

jobs:
  send-test:
    runs-on: ubuntu-latest
    steps:
      - name: Send test message to Discord
        run: |
          curl -H "Content-Type: application/json" \
          -X POST \
          -d '{"content":"🔥 TEST ยิงจาก GitHub Actions สำเร็จ!"}' \
          ${{ secrets.DISCORD_WEBHOOK }}
