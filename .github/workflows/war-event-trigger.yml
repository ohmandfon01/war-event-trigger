# Force schedule debug mode ⏰

name: War Event Trigger

on:
  schedule:
    - cron: "*/5 * * * *"   # ทุก 5 นาที (UTC)
  workflow_dispatch:

permissions:
  contents: read

jobs:
  send-time:
    runs-on: ubuntu-latest

    steps:
      - name: Send current Thai time to Discord
        run: |
          NOW_UTC_H=$(date -u +%H)
          NOW_UTC_M=$(date -u +%M)

          # แปลงเป็นเวลาไทย (UTC+7)
          H=$(( (10#$NOW_UTC_H + 7) % 24 ))
          M=$((10#$NOW_UTC_M))

          printf -v THAI_TIME "%02d:%02d" $H $M
          echo "Thai time now: $THAI_TIME"

          curl -H "Content-Type: application/json" \
            -X POST \
            -d "{\"content\":\"🕒 ระบบทำงานอยู่ ตอนนี้เวลาไทย ${THAI_TIME}\"}" \
            ${{ secrets.DISCORD_WEBHOOK }}
