name: 5s Spam Test

on:
  workflow_dispatch:

jobs:
  spam:
    runs-on: ubuntu-latest

    steps:
      - name: Send every 5 seconds
        run: |
          for i in {1..12}
          do
            NOW=$(date +%H:%M:%S)
            echo "Sending at $NOW"

            curl -H "Content-Type: application/json" \
              -X POST \
              -d "{\"content\":\"🔥 TEST ทุก 5 วิ เวลา $NOW\"}" \
              ${{ secrets.DISCORD_WEBHOOK }}

            sleep 5
          done
